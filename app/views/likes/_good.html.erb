<%= form_with(model: monthly_expense.likes.build, remote: true) do |f| %>
	<div><%= hidden_field_tag :monthly_expense_id, monthly_expense.id %></div>
  <%= button_tag(class: "btn btn-default btn-xs") do %>
  	<%= content_tag :span, "#{monthly_expense.likes_count}", class: "glyphicon glyphicon-heart-empty" %>
	<% end %>
<% end %>
